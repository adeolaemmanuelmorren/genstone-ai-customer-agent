# Testing

This folder owns fixtures and runtime harnesses. Test behavior must remain
separate from production routes and production entity identifiers.

Browser test interfaces belong in an authenticated frontend, not inline HTML
served by the Worker.

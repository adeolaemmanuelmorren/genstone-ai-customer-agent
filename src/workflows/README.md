# Workflows

This folder will own durable business timelines after workflow boundaries are
approved.

Use Workflows for multi-step external writes, retries, delayed callbacks, and
process recovery. Keep provider calls and SQL in services. Each external side
effect must have an idempotency rule that survives workflow retries.

There are no active Workflow bindings or classes yet.

# Library

This folder owns small product-local infrastructure such as structured logging,
error classification, retry policy, and workflow-step helpers.

It must not own GenStone business rules or provider-specific behavior.

# Entrypoints

Entrypoints own runtime surfaces and dispatch.

The current product exposes HTTP only. Add provider webhooks, queue consumers,
scheduled handlers, Durable Objects, or Workflow exports only when the runtime
contract requires them.

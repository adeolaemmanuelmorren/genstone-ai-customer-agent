# Types

This folder owns shared Worker environment and Cloudflare binding contracts.

Every binding referenced by runtime code must appear in the typed environment,
`wrangler.jsonc`, and the environment-variable documentation.
